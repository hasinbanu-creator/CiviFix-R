name: CiviFix E2E Appium Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest openpyxl
        
    - name: Run E2E Appium Test Suite
      run: |
        pytest -s appium_tests/test_mock_appium.py -v
        
    - name: Generate Execution Report
      run: |
        python appium_tests/generate_report.py
        
    - name: Upload Excel Report Artifact
      uses: actions/upload-artifact@v4
      with:
        name: CiviFix_Appium_Execution_Report
        path: CiviFix_Appium_Execution_Report.xlsx
